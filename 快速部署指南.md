# 快速部署指南

## 🚀 5分钟快速部署

### 步骤 1: 准备数据库（2分钟）

1. **创建 Neon 账户**
   - 访问 https://neon.tech
   - 注册/登录账户
   - 创建新项目
   - 复制数据库连接字符串

### 步骤 2: 部署到 Vercel（2分钟）

1. **推送代码到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/your-repo.git
   git branch -M main
   git push -u origin main
   ```

2. **在 Vercel 中部署**
   - 访问 https://vercel.com
   - 导入 GitHub 仓库
   - 添加环境变量 `DATABASE_URL`（你的 Neon 数据库连接字符串）
   - 点击 "Deploy"

### 步骤 3: 初始化数据库（1分钟）

1. **访问初始化 API**
   ```
   https://your-project.vercel.app/api/init
   ```

2. **验证初始化成功**
   - 应该看到 `{"success": true, "message": "数据库初始化成功"}`

### 步骤 4: 测试系统

1. **访问登录页面**
   ```
   https://your-project.vercel.app
   ```

2. **使用默认管理员账号登录**
   - 用户名: `admin`
   - 密码: `admin`

3. **测试功能**
   - 学生注册
   - 学生登录
   - 学生信息管理

## ✅ 完成！

系统已成功部署并可以使用了！

## 📚 详细文档

- [完整部署指南](./VERCEL_DEPLOYMENT_GUIDE.md)
- [部署检查清单](./部署检查清单.md)
- [测试说明](./测试说明.md)

## 🔧 常用命令

```bash
# 测试部署（Windows PowerShell）
.\test-deployment.ps1 -Url "https://your-project.vercel.app"

# 测试部署（Linux/Mac Bash）
chmod +x test-deployment.sh
./test-deployment.sh https://your-project.vercel.app
```

## 💡 提示

- 首次部署后必须访问 `/api/init` 初始化数据库
- 默认管理员账号: `admin` / `admin`
- 建议在生产环境中修改默认管理员密码

