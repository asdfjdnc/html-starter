# 网站测试说明

## ✅ 代码测试结果

所有代码测试已通过！

### 测试结果

- ✅ API 代码语法正确（8个文件）
- ✅ 前端 JavaScript 代码语法正确（4个文件）
- ✅ HTML 文件结构正确（4个文件）
- ✅ 配置文件格式正确（2个文件）

## 🚀 测试方法

### 方法一：本地测试前端（推荐）

1. **启动本地测试服务器**：

```bash
node test-server.js
```

2. **在浏览器中访问**：

- 登录页面: http://localhost:3000
- 注册页面: http://localhost:3000/register.html
- 管理员页面: http://localhost:3000/admin.html
- 学生页面: http://localhost:3000/student.html

**注意**: 本地测试只能测试前端页面显示，API 功能需要在 Vercel 上测试。

### 方法二：部署到 Vercel 测试（完整测试）

#### 1. 部署步骤

1. **将代码推送到 GitHub**：
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/your-repo.git
   git branch -M main
   git push -u origin main
   ```

2. **在 Vercel 中部署**：
   - 访问 https://vercel.com
   - 导入 GitHub 仓库
   - 配置环境变量 `DATABASE_URL`（Neon 数据库连接字符串）
   - 点击 "Deploy"

#### 2. 初始化数据库

部署完成后，访问：

```
https://your-domain.vercel.app/api/init
```

或者使用浏览器直接访问，应该看到：

```json
{
  "success": true,
  "message": "数据库初始化成功"
}
```

#### 3. 测试功能

**默认管理员账号**：
- 用户名: `admin`
- 密码: `admin`

**测试流程**：

1. **管理员登录测试**
   - 访问登录页面
   - 选择"管理员"
   - 输入用户名 `admin` 和密码 `admin`
   - 点击登录，应该跳转到管理员页面

2. **学生注册测试**
   - 访问注册页面
   - 填写学生信息（学号、密码、姓名、性别、年龄、班级、专业）
   - 点击注册，应该显示注册成功

3. **学生登录测试**
   - 访问登录页面
   - 选择"学生"
   - 输入注册时使用的学号和密码
   - 点击登录，应该跳转到学生信息页面

4. **学生信息管理测试（管理员）**
   - 以管理员身份登录
   - 查看学生列表
   - 添加新学生
   - 编辑学生信息
   - 删除学生
   - 搜索学生

## 📋 测试清单

### 前端功能

- [x] 登录页面显示正常
- [x] 注册页面显示正常
- [x] 管理员页面显示正常
- [x] 学生页面显示正常
- [x] 响应式设计正常
- [x] 中文显示正常
- [x] 表单验证正常
- [x] 用户类型选择器正常

### API 功能（需要在 Vercel 上测试）

- [ ] 数据库初始化
- [ ] 管理员登录
- [ ] 学生注册
- [ ] 学生登录
- [ ] 获取学生列表
- [ ] 添加学生
- [ ] 获取学生详情
- [ ] 更新学生信息
- [ ] 删除学生
- [ ] 搜索学生

### 安全性

- [ ] 密码加密存储
- [ ] 未授权访问被阻止
- [ ] SQL 注入防护
- [ ] XSS 防护

## 🔍 问题排查

如果遇到问题，请检查：

1. **数据库连接问题**
   - 检查 `DATABASE_URL` 环境变量是否正确配置
   - 检查 Neon 数据库是否正常运行
   - 查看 Vercel 函数日志

2. **API 请求失败**
   - 打开浏览器开发者工具（F12）
   - 查看 Console 标签页的错误信息
   - 查看 Network 标签页的请求状态

3. **中文乱码**
   - 检查数据库编码设置
   - 检查 API 响应头
   - 检查 HTML 文件的 charset 设置

4. **页面显示问题**
   - 检查 CSS 文件是否正常加载
   - 检查 JavaScript 文件是否正常加载
   - 清除浏览器缓存后重试

## 📝 测试报告模板

测试完成后，请记录：

```
测试日期: ___________
测试环境: ___________
浏览器: ___________
操作系统: ___________

测试结果:
- 前端页面: ✅ / ❌
- API 功能: ✅ / ❌
- 数据库连接: ✅ / ❌
- 安全性: ✅ / ❌

发现的问题:
1. ___________
2. ___________

问题解决方案:
1. ___________
2. ___________
```

## 🎯 下一步

1. 如果本地测试通过，可以部署到 Vercel 进行完整测试
2. 如果部署后测试通过，系统就可以正式使用了
3. 建议在生产环境中修改默认管理员密码

## 💡 提示

- 本地测试服务器只能测试前端页面，不能测试 API 功能
- API 功能需要在 Vercel 上测试，因为需要连接 Neon 数据库
- 确保在 Vercel 中正确配置了 `DATABASE_URL` 环境变量
- 首次部署后必须访问 `/api/init` 初始化数据库

