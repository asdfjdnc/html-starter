# 快速推送到 GitHub

## 🚀 方法一：使用批处理文件（推荐）

直接双击运行：
```
push-to-github.bat
```

或者 PowerShell 中运行：
```powershell
.\push-to-github.bat
```

## 🚀 方法二：手动执行命令

### 步骤 1: 在 GitHub 上创建仓库

1. 访问: https://github.com/new
2. 仓库名: `student-management-system`（或你喜欢的名称）
3. **不要**勾选 "Initialize this repository with a README"
4. 点击 "Create repository"

### 步骤 2: 添加远程仓库并推送

在 PowerShell 中执行（**替换 YOUR_USERNAME 和 YOUR_REPO_NAME**）：

```powershell
# 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 推送到 GitHub
git push -u origin main
```

**示例**：
```powershell
git remote add origin https://github.com/john/student-management-system.git
git push -u origin main
```

### 步骤 3: 身份验证

如果提示需要身份验证：

1. **获取 Personal Access Token**
   - 访问: https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 选择 `repo` 权限
   - 生成并复制 token

2. **推送时使用**
   - 用户名: 你的 GitHub 用户名
   - 密码: 粘贴刚才复制的 token

## ✅ 验证推送成功

推送成功后，访问你的 GitHub 仓库，应该能看到所有文件了！

## 📝 下一步

推送完成后，按照 [QUICK_START.md](./QUICK_START.md) 部署到 Vercel。

## ❓ 遇到问题？

- **远程仓库已存在**: 
  ```powershell
  git remote remove origin
  git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
  ```

- **推送被拒绝**: 确认仓库已创建，且没有初始化 README

- **身份验证失败**: 使用 Personal Access Token，不是 GitHub 密码

## 📚 相关文档

- [PUSH_TO_GITHUB.md](./PUSH_TO_GITHUB.md) - 详细说明
- [QUICK_START.md](./QUICK_START.md) - 快速部署指南
- [NEXT_STEPS.md](./NEXT_STEPS.md) - 完整步骤

